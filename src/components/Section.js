import styled from "styled-components"

export default styled.section`
  padding: 4rem 10vw;
  display: flex;
  flex-direction: column;
  align-items: center;
  &:nth-of-type(odd) {
    background: #ededed;
  }
`
